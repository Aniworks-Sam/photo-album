import { consumer } from "../services/Consumer";
import { HttpMethod } from "../services/Endpoint";
import { ISettings } from "../settings";


export const getEndpoints = ({ baseUrl }: ISettings) => ({

});